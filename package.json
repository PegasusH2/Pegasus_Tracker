{
  "name": "pegasus-tracker-tests",
  "private": true,
  "description": "Infraestructura de tests SOLO para desarrollo — la app en sí sigue siendo HTML/CSS/JS vanilla sin build, servida tal cual desde GitHub Pages. Este package.json no se despliega ni lo carga la PWA.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "dexie": "^4.0.0",
    "fake-indexeddb": "^6.0.0"
  }
}
